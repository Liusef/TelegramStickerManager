<Page
    x:Class="ReunionApp.Pages.Loading"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ReunionApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <Image Grid.Row="1"  Source="..\Assets\app.png" Width="400" VerticalAlignment="Center" Margin="0,24,0,0"/>
        <muxc:ProgressBar Grid.Row="2" x:Name="LoadingProgressRing" Width="500" IsIndeterminate="true" VerticalAlignment="Bottom" Margin="50,0,50,10"/>
        <TextBlock Grid.Row="3" x:Name="LoadingProgressText" VerticalAlignment="Top" Text="Starting" HorizontalAlignment="Center" Style="{StaticResource SubtitleTextBlockStyle}" />
    </Grid>
</Page>
