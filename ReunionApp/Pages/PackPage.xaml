<Page
    x:Class="ReunionApp.Pages.PackPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ReunionApp.Pages"
    xmlns:types="using:TgApi.Types"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="24, 24, 0, 0"  Spacing="12">
            <Button Content="Back" Click="Button_Click"/>
            <StackPanel Padding="24" Spacing="8" CornerRadius="12"
                    Background="{ThemeResource SystemRevealListLowColor}">
                <Image Source="{x:Bind local:PackPage.PackThumb(pack.EnsuredThumb)}" Width="100" />
                <TextBlock Text="{x:Bind pack.Title}" FontSize="24" FontWeight="Bold" TextTrimming="CharacterEllipsis" TextWrapping="Wrap"/>
                <TextBlock Text="{x:Bind pack.Name}" FontWeight="Light" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="1" x:Name="Loading" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="24" Visibility="Visible">
            <ProgressRing Grid.Row="2" Width="60" Height="60"/>
            <TextBlock Text="Loading" HorizontalAlignment="Center" FontSize="18" FontWeight="SemiBold"/>
        </StackPanel>
        
        <GridView 
            x:Name="StickerGrid" 
            Grid.Column="1" 
            Visibility="Collapsed"
            ItemsSource="{x:Bind stickers}"
            HorizontalAlignment="Center">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="types:Sticker">
                    <StackPanel Padding="12, 12, 12, 12" Spacing="10">
                        <Image Source="{x:Bind local:PackPage.StickerImage(Filename)}" Width="120"/>
                        <TextBlock Text="{x:Bind Emojis}" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        
    </Grid>
</Page>
