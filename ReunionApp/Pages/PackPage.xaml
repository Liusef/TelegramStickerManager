<Page
    x:Class="ReunionApp.Pages.PackPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ReunionApp.Pages"
    xmlns:project="using:ReunionApp"
    xmlns:types="using:TgApi.Types"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:ReunionApp.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="24, 24, 0, 0"  Spacing="12">
            <Button Content="Back" Click="Button_Click"/>
            <StackPanel Spacing="8">
                <Image Source="{x:Bind local:PackPage.PackThumb(pack.EnsuredThumb)}" Width="100" HorizontalAlignment="Center"/>
                <TextBlock Text="{x:Bind pack.Title}" FontSize="24" FontWeight="Bold" TextTrimming="CharacterEllipsis" TextWrapping="Wrap"/>
                <TextBlock Text="{x:Bind pack.Name}" FontWeight="Light" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
            <StackPanel Margin="0, 24, 0, 0" HorizontalAlignment="Stretch">
                <Button Content="Add Stickers" HorizontalContentAlignment="Center" HorizontalAlignment="Stretch"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="1" x:Name="Loading" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="24" Visibility="Visible">
            <ProgressRing Grid.Row="2" Width="60" Height="60"/>
            <TextBlock Text="Loading" HorizontalAlignment="Center" FontSize="18" FontWeight="SemiBold"/>
        </StackPanel>

        <GridView 
            x:Name="StickerGrid" 
            Grid.Column="1" 
            Visibility="Collapsed"
            ItemsSource="{x:Bind stickers}"
            HorizontalAlignment="Center"
            Margin="16"
            Padding="8"
            CornerRadius="16"
            Background="{ThemeResource SystemRevealListLowColor}">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="types:Sticker">
                    <StackPanel Padding="12" Spacing="12" MinHeight="163">
                        <controls:ImageAsync x:Name="StickerImg" DecodeWidth="108" Width="108" Height="108" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                             Path="{x:Bind local:PackPage.StickerPath(Filename)}"/>
                        <TextBlock Text="{x:Bind Emojis}" HorizontalAlignment="Center" VerticalAlignment="Bottom" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
